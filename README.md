# simple-php-payment-form
This is a simple dockerized PHP application

### Startup
Start the application with the following command:

[`docker-compose up`]

After containers started navigate to localhost:8000 in your browser.
